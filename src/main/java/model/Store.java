package model;import lombok.Data;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.stereotype.Component;import repositories.ItemRepository;import service.IFastLane;import service.IJsutHaveALook;import service.ILane;import service.IStore;import javax.persistence.GeneratedValue;import javax.persistence.GenerationType;import javax.persistence.Id;@Component@Datapublic class Store implements IFastLane, IJsutHaveALook, IStore, ILane {    @Autowired    ItemRepository itemRepository;    @Autowired    IFastLane FastLane;    @Autowired    IStore store;    @Autowired    IJsutHaveALook havelook;    @Autowired    ILane lane ;    @Override    public void oneShotOrder(Item item, int amount) {    }    @Override    public double getPrice(Item item) {        return item.getPrice();    }    @Override    public boolean isAvailable(Long itemId) {        Item item = itemRepository.getById(itemId);        if(!item.isAvailable()){            return false;        }        return true;    }    @Override    public void addItemToCart(Long itemId) {        Item item = itemRepository.getById(itemId);        if(item.isAvailable()){            itemRepository.save(item);        }    }    public void pay(float price) {    }}